# ~/.config/picom/picom.conf

backend = "xrender";
vsync = true;

# Fading
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;

shadow = false;
#shadow-radius = 7;
#shadow-opacity = 0.5;

# Avoid flicker on window open/close
unredir-if-possible = false;
